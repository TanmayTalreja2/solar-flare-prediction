dataset:
  name: solar_flare_v1

  start_date: "2011-01-01"
  end_date: "2025-12-31"

  cadence_minutes: 12
  forecast_window_hours: 24

  positive_classes:
    - M
    - X

  max_storage_gb: 30

storage:
  raw_dir: data/raw
  interim_dir: data/interim
  processed_dir: data/processed

  sharp_dir: data/raw/sharp
  goes_dir: data/raw/goes
  magnetogram_dir: data/raw/magnetograms

images:
  format: png
  target_width: 512